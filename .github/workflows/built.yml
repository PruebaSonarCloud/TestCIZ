name: Swift

on:
  push:
    branches: [ "main" ]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:

    runs-on: macos-14

    steps:
    - uses: actions/checkout@v3

    - name: Xcode Setup
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '15.3'
    
    #- uses: actions/checkout@v3
    #- uses: NiftyStack/install-cocoapods-action@1.0.0
    #- run: pod install
    
    - name: Build
      run: |
        xcodebuild build -project TestCIZ.xcodeproj -scheme TestCIZ clean build -sdk iphoneos CODE_SIGNING_ALLOWED=No
    - name: Run tests
      run: |
        xcodebuild test -project TestCIZ.xcodeproj -scheme TestCIZ clean build -sdk iphoneos -destination "platform=iOS Simulator,OS=17.4,name=iPhone 15" CODE_SIGNING_ALLOWED=No

